
Function Subfunction(ByRef ByVal art() As Double)
	Dim a = 5
	'Static arrayVal(1,2) = { }
	DO While a < 10
		a = a \= (2 + 1)*1
	LOOP
	
	Do UNTIL a > 10
		a = a + (2 + 1)*1
		EXIT Do;
	LOOP
	
	Dim ces = TRUE
	Return ces
End Function